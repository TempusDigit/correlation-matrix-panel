export const NUMERIC_FIELD_START = 1;

export const TRUNCATE_OPTIONS = {
    length: 20,
    omission: '...',
};

export const TEST_SELECTORS = {
    plotlyChart: {
        root: 'test-data-plotly-chart',
    },
};
